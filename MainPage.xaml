<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamlCompile"
             x:DataType="local:TestVm"
             BindingContext="{local:TestVm}"
             x:Class="XamlCompile.MainPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="{Binding Missing1}"/>
        <ToolbarItem Command="{Binding Missing2}"/>
    </ContentPage.ToolbarItems>
    <Label Text="{Binding TestString}"/>
</ContentPage>
